# Unclassified branches a reading accounted for, per claim, on
# x86_64-unknown-linux-gnu. Read under rustc 1.95.0 and 1.98.1 with the
# one-codegen-unit build the script pins; each count is the largest that claim
# has shown across them. The newer compiler inlines SHA-256's finalisation into
# the tag comparison, for instance, which brings its padding decision on the
# message length — a public value — into the claim.
#
# ml-kem-decaps went from 35 to 65 under rustc 1.95.0 when the crate gained
# its edwards25519 modules: the build is one codegen unit, so more code
# elsewhere changes what is inlined here. All 65 were read: the coefficient
# loops at 512 and 2048, the parameter-set selection at k of 1, 2 and 3, the
# byte-length guards at 12 and 384, the NTT's zeta index bounds, memcpy's
# chunking of a public length, and SHA3-512's 72-byte rate. None reads a
# secret polynomial or the implicit-rejection selection.
#
# `scripts/ct_codegen.sh --accept` rewrites this file; the header stays.
tag-comparison 3
aes128-ct 0
camellia128-ct 0
cast128-ct 22
des-ct 0
grasshopper-ct 0
magma-ct 0
present80-ct 0
seed-ct 1
sm4-ct 0
twofish128-ct 40
serpent128 0
x25519-ladder 1
x448-ladder 3
x25519-agree 1
ed25519-sign 9
ml-kem-decaps 65
ntru-hps509-decaps 19
chacha20-keystream 17
poly1305-mac 2
chacha20poly1305-open 4
